[tool:pytest]

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
# Additional command-line options passed by default
# -v = verbose output
# --tb=short = shorter tracebacks when tests fail
addopts = -v --tb=short